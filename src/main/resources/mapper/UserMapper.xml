<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.blog.mapper.UserMapper">

    <select id="getInfo" parameterType="String" resultType="com.example.blog.bean.UserBean">
        SELECT * FROM user WHERE name = #{name} AND password = #{password}
    </select>

    <select id="getUserList" parameterType="String" resultType="com.example.blog.bean.UserBean">
        select * from user order by user_id desc
    </select>

</mapper>